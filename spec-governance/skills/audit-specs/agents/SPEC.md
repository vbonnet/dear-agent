# Audit SPEC skill agent metadata specification

## EARS Requirements

**AUDIT-SPEC-AGENT-01** When an OpenAI-compatible interface advertises the audit skill, the system shall identify `audit-specs`, describe its read-only consolidation audit, and provide a default prompt that delegates to the canonical skill workflow.

## BDD Traceability

- Feature: `agm/test/bdd/features/spec_governance_tooling.feature`
