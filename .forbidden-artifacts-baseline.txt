# Forbidden-artifact baseline — grandfathered, pre-existing routing debt.
#
# These temporal artifacts predate enforcement and are exempted from the
# CI whole-tree scan (routing-guard --all --baseline) so it can block NEW
# leaks without forcing an out-of-scope migration.
#
# This list is SHRINK-ONLY. When you migrate one of these to
# engram-research, delete its line here. Do NOT add new entries — a new
# violation must be fixed, not baselined. See the 2026-06-19 Wayfinder
# leak DEAR retro (engram-research) and .dear-agent.yml forbidden-paths.
#
# NOTE: docs/design/{anti-stall,graceful-exit,substrate-diagnostic}.md are
# the documented living-doc exception (see CLAUDE.md / .dear-agent.yml).

docs/design/anti-stall.md
docs/design/graceful-exit.md
docs/design/substrate-diagnostic.md
