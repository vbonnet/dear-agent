name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  ci:
    name: Build & Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          cache: true
          cache-dependency-path: "go.sum"

      - name: Download dependencies
        run: go mod download

      - name: Build
        run: |
          GIT_COMMIT=$(git rev-parse --short HEAD)
          BUILD_DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          LDFLAGS="-s -w -X main.Version=2.0.0-dev -X main.GitCommit=${GIT_COMMIT} -X main.BuildDate=${BUILD_DATE} -X main.BuiltBy=ci"
          go build -ldflags="${LDFLAGS}" ./agm/cmd/agm
          go build -ldflags="${LDFLAGS}" ./agm/cmd/agm-reaper
          go build -ldflags="${LDFLAGS}" ./agm/cmd/agm-mcp-server
          go build ./...

      - name: Vet
        run: go vet ./...

      - name: Test
        run: go test -race -count=1 ./...
        env:
          # Skip tmux-dependent tests on macOS CI
          CI_SKIP_TMUX: ${{ matrix.os == 'macos-latest' && 'true' || 'false' }}

      - name: Lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest
