name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  ci:
    name: Build & Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.9"
          cache: true
          cache-dependency-path: "go.sum"

      - name: Download dependencies
        run: go mod download

      - name: Build
        run: |
          GIT_COMMIT=$(git rev-parse --short HEAD)
          BUILD_DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          LDFLAGS="-s -w -X main.Version=2.0.0-dev -X main.GitCommit=${GIT_COMMIT} -X main.BuildDate=${BUILD_DATE} -X main.BuiltBy=ci"
          go build -ldflags="${LDFLAGS}" -o /tmp/agm ./agm/cmd/agm
          go build -ldflags="${LDFLAGS}" -o /tmp/agm-reaper ./agm/cmd/agm-reaper
          go build -ldflags="${LDFLAGS}" -o /tmp/agm-mcp-server ./agm/cmd/agm-mcp-server
          go build ./...

      - name: Install binaries for e2e tests
        run: |
          mkdir -p $HOME/go/bin
          cp /tmp/agm $HOME/go/bin/agm
          cp /tmp/agm-reaper $HOME/go/bin/agm-reaper
          cp /tmp/agm-mcp-server $HOME/go/bin/agm-mcp-server

      - name: Vet
        run: go vet ./...

      - name: Test
        run: go test -race -count=1 -short ./...
        env:
          # Skip tmux-dependent tests on macOS CI
          CI_SKIP_TMUX: ${{ matrix.os == 'macos-latest' && 'true' || 'false' }}

      - name: Lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest
