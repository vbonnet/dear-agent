// Package phaseengram maps Wayfinder SDLC phases to their corresponding
// engram workflow files. It resolves canonical phase identifiers (for example,
// "CHARTER" and "PROBLEM") to engram file paths and validates content hashes.
package phaseengram
