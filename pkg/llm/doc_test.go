// Package llm is a doc-only umbrella; its sub-packages hold the logic.
package llm

import "testing"

func TestPackageDoc(t *testing.T) {
	// pkg/llm is an umbrella with no logic of its own.
	// Real implementations live in pkg/llm/auth, pkg/llm/config, etc.
}
