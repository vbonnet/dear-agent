# Test: csm --version should display version information
# This is a simple smoke test to verify the csm binary works

exec csm --version
stdout 'csm version'

# Test: csm without arguments should show help
# Note: This currently uses a placeholder implementation
# Full integration requires exporting rootCmd from cmd/csm/main.go
exec csm
stdout 'Usage:'
stdout 'Claude Session Manager'
