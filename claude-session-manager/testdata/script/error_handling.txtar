# Test: Error handling and validation

mkdir $HOME/sessions

# Test: Invalid manifest format should fail gracefully
cp invalid.yaml $HOME/sessions/

exec agm list --test
stderr 'failed to parse'
! stdout 'invalid'

# Test: Missing required fields validation
cp missing-fields.yaml $HOME/sessions/

! exec agm list --test
stderr 'validation'

# Test: Corrupted manifest file
cp corrupted.yaml $HOME/sessions/

! exec agm list --test
stderr 'parse'

# Test: Permission denied on sessions directory
mkdir $HOME/no-access
chmod 000 $HOME/no-access

! exec agm list --test --sessions-dir $HOME/no-access
stderr 'permission denied'

# Restore permissions for cleanup
chmod 755 $HOME/no-access

# Test: Archive non-existent session
! exec agm archive does-not-exist --test
stderr 'not found'

# Test: Resume non-existent session
! exec agm resume does-not-exist --test
stderr 'not found'

# Test: Invalid session name characters
! exec agm new "invalid/name" --test --detached
stderr 'invalid'

# Test: Empty session name
! exec agm new "" --test --detached
stderr 'required'

# Test: Session name conflict (already exists)
cp existing.yaml $HOME/sessions/

! exec agm new existing --test --detached
stderr 'already exists'

-- invalid.yaml --
not_a_valid: manifest
missing: required_fields

-- missing-fields.yaml --
schema_version: "2"
name: missing-fields
# Missing: session_id, created_at, updated_at, tmux.session_name

-- corrupted.yaml --
schema_version: "2"
session_id: corrupted-001
name: corrupted-session
  invalid_indentation: here
    more_bad_yaml: true

-- existing.yaml --
schema_version: "2"
session_id: sess-existing-001
name: existing
created_at: 2024-01-01T12:00:00Z
updated_at: 2024-01-01T12:00:00Z
context:
  project: /tmp/test
tmux:
  session_name: existing
agent: claude
